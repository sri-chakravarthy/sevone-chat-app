{
  "mcpServers": {
    "sevone-mysql": {
      "transport": "sse",
      "url": "${SEVONE_MCP_URL}",
      "headers": {
        "Authorization": "Bearer ${SEVONE_MCP_AUTH_TOKEN}"
      },
      "timeout": 30000
    }
  }
}